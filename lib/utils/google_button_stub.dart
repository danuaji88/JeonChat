import 'package:flutter/widgets.dart';

/// Stub non-web — path Android/iOS pakai tombol custom + signIn() imperatif
/// biasa (lihat SocialLoginButtons), tidak butuh widget ini sama sekali.
Widget buildGoogleRenderButton() => const SizedBox.shrink();
